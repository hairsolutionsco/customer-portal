# Use legacy peer deps to avoid strict peer dependency conflicts
# This is useful for Railway and other CI/CD environments
legacy-peer-deps=false

# Ensure we're using npm v8+ behavior
engine-strict=false

# Production builds should not include dev dependencies
production=false
