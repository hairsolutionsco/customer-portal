# Copy keys into Hostinger Docker Manager → Environment (do not commit real values).
# URL will be: https://${COMPOSE_PROJECT_NAME}.${TRAEFIK_HOST}

TRAEFIK_HOST=srv1554656.hstgr.cloud
COMPOSE_PROJECT_NAME=customer-portal
TZ=America/Sao_Paulo

POSTGRES_PASSWORD=change-me-long-random

NEXTAUTH_URL=https://customer-portal.srv1554656.hstgr.cloud
NEXTAUTH_SECRET=run-openssl-rand-base64-32
